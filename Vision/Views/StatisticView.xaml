<UserControl x:Class="Vision.Views.StatisticView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="时间范围:" VerticalAlignment="Center" Margin="0,0,4,0"/>
            <ComboBox Width="100" SelectedIndex="{Binding SelectedRangeIndex}">
                <ComboBoxItem Content="最近1小时"/>
                <ComboBoxItem Content="最近4小时"/>
                <ComboBoxItem Content="最近24小时"/>
            </ComboBox>
            <Button Content="刷新" Command="{Binding RefreshCmd}" Margin="8,0,0,0" Padding="12,2"/>
            <TextBlock Text="| 实时:" VerticalAlignment="Center" Margin="16,0,4,0"/>
            <TextBlock Text="{Binding TotalCount, StringFormat=总数:{0}}" VerticalAlignment="Center" Margin="4,0"/>
            <TextBlock Text="{Binding OkCount, StringFormat=合格:{0}}" VerticalAlignment="Center" Margin="4,0"/>
            <TextBlock Text="{Binding NgCount, StringFormat=不合格:{0}}" VerticalAlignment="Center" Margin="4,0" RenderTransformOrigin="0.5,0.5">
                <TextBlock.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform/>
                        <RotateTransform Angle="-5.141"/>
                        <TranslateTransform/>
                    </TransformGroup>
                </TextBlock.RenderTransform>
            </TextBlock>
            <TextBlock Text="{Binding Yield, StringFormat=良率:{0:F1}%}" VerticalAlignment="Center" Margin="4,0" FontWeight="Bold"/>
        </StackPanel>

        <!-- 实时计数卡片 -->
        <Border Grid.Row="1" Margin="0,0,0,8" Padding="8" CornerRadius="4"
                Background="#F0F4F8">
            <TextBlock HorizontalAlignment="Center">
                <Run Text="总检测数: " FontSize="14"/>
                <Run Text="{Binding TotalCount}" FontSize="14" FontWeight="Bold"/>
                <Run Text="  |  合格: " FontSize="14"/>
                <Run Text="{Binding OkCount}" FontSize="14" FontWeight="Bold" Foreground="Green"/>
                <Run Text="  |  不合格: " FontSize="14"/>
                <Run Text="{Binding NgCount}" FontSize="14" FontWeight="Bold" Foreground="Red"/>
                <Run Text="  |  良率: " FontSize="14"/>
                <Run Text="{Binding Yield, StringFormat={}{0:F1}%}" FontSize="14" FontWeight="Bold"/>
            </TextBlock>
        </Border>

        <!-- LiveCharts 双曲线图 -->
        <Border Grid.Row="2" BorderBrush="Black" BorderThickness="1">
            <lvc:CartesianChart Series="{Binding Series}" YAxes="{Binding YAxes}" />
        </Border>
    </Grid>
</UserControl>
